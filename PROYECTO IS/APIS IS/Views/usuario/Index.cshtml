@model IEnumerable<APIS_IS.Models.usuario>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.contrasenia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.contrasenia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.pk_id_usuario }) |
            @Html.ActionLink("Details", "Details", new { id=item.pk_id_usuario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.pk_id_usuario })
        </td>
    </tr>
}

</table>
